## 2026-07-18 - PythonWeb 项目初始化

### 项目创建
- 基于 `full-stack-fastapi-template` 构建单体 FastAPI Web 服务
- 工作目录: `e:\cywork\github\PythonWeb`
- Python 3.14 (D:\dev\Python\Python314\)
- PostgreSQL 16 (D:\dev\PostgreSQL\16\bin\)

### 数据库配置
- 数据库名: `pythonweb`
- 用户: `postgres`
- 密码: `123456`
- 主机: `localhost:5432`
- ORM: SQLModel (基于 SQLAlchemy + Pydantic)
- 迁移: Alembic (单一合并迁移 `0001_initial`)

### API 路由
- `/api/v1/login/access-token` - 登录获取JWT
- `/api/v1/users/` - 用户CRUD
- `/api/v1/items/` - 项目CRUD
- `/api/v1/utils/health-check/` - 健康检查
- `/api/v1/private/users/` - 本地环境专用

### 超级用户
- 邮箱: `admin@example.com`
- 密码: `admin123`

### 启动命令
```bash
cd e:\cywork\github\PythonWeb
chcp 65001
set PYTHONPATH=%CD%
set NO_COLOR=1
py -3.14 -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```
或使用 `scripts\start.bat`

### 关键修改
- `config.py`: `env_file` 改为 `".env"` (适配单体部署)
- `main.py`: 移除 Sentry 依赖
- 5个模板迁移合并为单个 `0001_initial.py`
- Python 3.14 需要设置 `PYTHONPATH=.` 才能导入本地 `app` 包
- Windows 控制台需 `chcp 65001` + `NO_COLOR=1` 避免 emoji 编码问题

### Conda py312 环境
- Conda 路径: `d:\dev\anaconda3`
- py312 环境: `D:\dev\conda_envs\py312` (Python 3.12.13)
- 执行命令前需: `call d:\dev\anaconda3\Scripts\activate.bat py312`
- `scripts/start.bat` 已更新为使用 conda py312
- `requirements.txt` 在 py312 下已安装全部依赖

### 测试用例 (2026-07-18)
- 为所有 API 接口编写了 52 个测试用例，全部通过
- 测试文件位于 `tests/`，使用 `httpx.AsyncClient` + `pytest`
- 运行命令: `call d:\dev\anaconda3\Scripts\activate.bat py312 && set PYTHONPATH=. && python -m pytest tests/ -v`
- 依赖: `pytest`, `httpx`

### 启动服务
- 使用 `start \"PythonWeb Server\" cmd /c ...` 在新窗口启动，保持后台运行
- 健康检查: `http://localhost:8000/api/v1/utils/health-check/`
